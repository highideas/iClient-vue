version: '2'
services:
    iClientVue:
        image: node:4.6.0
        container_name: iClientVue
        restart: always
        ports:
         - "8080:8080"
        volumes:
         - ./:/client
        tty: true

